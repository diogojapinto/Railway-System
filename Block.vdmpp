class Block is subclass of TrackElement
types
values
instance variables
	train: [Railway`Train] := nil;
operations
	public placeTrain: Railway`Train ==> ()
	placeTrain(newTrain) == (
		train := newTrain;
	)
	pre
		train = nil
	post
		train <> nil;
functions
traces
end Block